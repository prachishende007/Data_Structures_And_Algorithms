#include<bits/stdc++.h>
using namespace std;

int main()
{
    deque<int>dq;
    dq.push_back(1);
    dq.emplace_back(3);

    // rest operations are same as vectore
    // begin, end, rbegin, rend, clear, insert, size, swap
    
}